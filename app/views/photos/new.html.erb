<h1>New photo</h1>

<% form_for @photo, :html => { :multipart => true } do |f| %>
  <%= render(:partial => "form", :locals => { :f => f } ) %>
  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', photos_path %>